{
  "type": "object",
  "description": "Criteria to filter and manage user job applications",
  "properties": {
    "userId": {
      "type": "integer",
      "description": "Filter by user ID"
    },
    "jobId": {
      "type": "string",
      "description": "Filter by job ID (e.g., JOB-2026-002)"
    },
    "status": {
      "type": "string",
      "enum": ["Applied", "Under Review", "Interview Scheduled", "Rejected", "Accepted", "Withdrawn"],
      "description": "Application status"
    },
    "appliedDate": {
      "type": "string",
      "format": "date-time",
      "description": "Date when the application was submitted (ISO 8601 format)"
    },
    "appliedDateFrom": {
      "type": "string",
      "format": "date-time",
      "description": "Filter applications from this date (ISO 8601 format)"
    },
    "appliedDateTo": {
      "type": "string",
      "format": "date-time",
      "description": "Filter applications until this date (ISO 8601 format)"
    },
    "notes": {
      "type": "string",
      "description": "Additional notes or comments about the application"
    },
    "operator": {
      "type": "string",
      "enum": ["AND", "OR"],
      "default": "AND",
      "description": "Logical operator to combine filter conditions"
    }
  },
  "additionalProperties": false
}
